# Rewrap

<!-- dankg:depends target=wrapped.md#wrapped quote="never follows symlinks" -->

Same words as `wrapped.md#wrapped`, just wrapped across lines there. This
must stay fresh: whitespace, not wording, is what moved.
